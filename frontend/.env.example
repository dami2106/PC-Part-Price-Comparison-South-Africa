# Copy this to .env.local for local overrides.
# These are injected at build time by Vite.

# Leave empty locally — Vite's dev proxy handles /api → localhost:8000
VITE_API_URL=

# Leave empty locally — only set when building for GitHub Pages
VITE_BASE_PATH=
